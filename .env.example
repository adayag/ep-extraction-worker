# Server configuration
NODE_ENV=production
PORT=3001

# Authentication
EXTRACTION_SECRET=your-32-character-secret-here

# MediaFlow proxy URL (include api_password if needed)
MEDIAFLOW_PROXY_URL=https://mediaflow.example.com?api_password=xxx

# Browser configuration
CHROME_PATH=/usr/bin/chromium

# Concurrency (not used yet, for future pooling)
MAX_CONCURRENT=3
